"""Phase 6 media domain: durable assets and human need-satisfactions.

A satisfied media need is an explicit, audited HUMAN binding of one brief
media need to one immutable content-addressed asset — never a flag and
never an automatic effect of generation (ADR 0004). Bytes live only in
the ContentOS-owned store; Konsepthane is never touched.
"""
