export default function HomePage() {
  return (
    <section className="panel" aria-labelledby="foundation-title">
      <h1 id="foundation-title">ContentOS</h1>
      <p>
        Private internal control panel for the Konsepthane editorial system.
      </p>
      <p>The admin foundation environment is running.</p>
      <p className="muted">
        Live backend state is intentionally not shown yet; the real status view
        arrives in a later foundation step.
      </p>
    </section>
  );
}
